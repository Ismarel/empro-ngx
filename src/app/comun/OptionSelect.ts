/**
 * Clase util que contiene value y label ambos de tipo string, utilizados en en select (simple o multiple),
 * se puede omitir la clase y crear el json manualmente
 */
export class OptionSelect {
    value: string;
    label: string;
}
